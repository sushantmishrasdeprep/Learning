# Learning

This repository contains learning materials and code examples.

## Java Setup

Java development environment setup instructions are available in the [java/SETUP.md](java/SETUP.md) file.

Quick start:
1. Install Java (see `java/SETUP.md` for instructions)
2. Compile: `cd java && ./compile.sh`
3. Run: `./run.sh help`
